-- Черновик техники заводится до выбора категории (ТЗ §2.5, шаг 1 визарда —
-- это как раз выбор). Пока категория не выбрана, поле пустое, поэтому
-- ограничение NOT NULL здесь мешало: карточка не создавалась вовсе.
ALTER TABLE catalog.equipment ALTER COLUMN category_id DROP NOT NULL;
