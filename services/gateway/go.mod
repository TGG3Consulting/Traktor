module traktor/gateway

go 1.24

// Только стандартная библиотека. Общий пакет проверки JWT (authjwt) вынесем в
// отдельный модуль, когда подключим приватный module-proxy; пока — локальная
// проверка в internal/jwks (дублирование минимально).
