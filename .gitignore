# Dart / Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
**/*.g.dart
**/*.freezed.dart
# Сгенерированные локализации (создаются flutter gen-l10n; .arb — исходники, остаются)
apps/mobile/lib/l10n/app_localizations*.dart
coverage/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Secrets — НИКОГДА не коммитить (только Secret Manager)
*.env
*.env.*
**/service-account*.json
**/*-key.json
infra/terraform/**/*.tfvars
infra/terraform/**/.terraform/
infra/terraform/**/*.tfstate*
!infra/terraform/**/*.tfvars.example

# Go
services/**/bin/
services/**/*.test

# Служебные скрипты сборки: логи и скачанные архивы (сами .bat/.ps1 — в репозитории)
scripts/_out/

# Архивы, оставшиеся от облачной сессии (можно удалить вручную)
_deliver*.tar.gz

# Временная папка для удаления вручную (мост не может rm)
_to_delete/
