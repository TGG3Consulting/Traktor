refactor(notifications): официальный Firebase Admin SDK вместо ручного FCM

Ручной HTTP-клиент к FCM v1 и самописный кэш токена metadata-сервера
заменены на firebase.google.com/go/v4 (правило 23). Интерфейс push.Provider
не изменился, fake-провайдер для разработки остался. Учётные данные берутся
из окружения (сервисный аккаунт Cloud Run) — ключей в репозитории нет.

Роутер переведён на chi/v5, хранилище — на pgx/v5 с миграциями через
golang-migrate (своя таблица версий схемы). Dockerfile — Go 1.25.
